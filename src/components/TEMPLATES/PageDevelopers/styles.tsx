import styled from "styled-components";

export const Wrapper = styled.div`
  width: 100%;
  height: 100vh;
  .conteudo {
    height: 100%;
    position: absolute;
    z-index: 10;
    overflow-y: hidden;
  }
`;
